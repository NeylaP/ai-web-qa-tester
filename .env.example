# Copy this file to .env and fill in your values
# Only one AI provider key is required — the tool auto-detects which one to use

# OpenAI (preferred for --enrich flag)
OPENAI_API_KEY=sk-proj-...

# Anthropic (alternative for --enrich flag)
# ANTHROPIC_API_KEY=sk-ant-...
