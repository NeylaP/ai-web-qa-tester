# scanner

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build scanner` to build the library.

## Running unit tests

Run `nx test scanner` to execute the unit tests via [Vitest](https://vitest.dev/).
