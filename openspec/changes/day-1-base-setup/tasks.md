# Tasks: Day 1 Base Setup

## Phase 1: Compatibility Spike

- [x] 1.1 Create disposable workspace in temp dir: `npx create-nx-workspace@21.x.y spike --preset=ts --pm=npm`
- [x] 1.2 Inspect real Angular generator options: `npx nx g @nx/angular:application --help` — record exact `--unitTestRunner` flag name and available values
- [x] 1.3 Scaffold Angular app in spike using confirmed flag; install `@analogjs/vitest-angular` and required Analog packages at compatible versions
- [x] 1.4 Write one `TestBed.createComponent()` test in spike `apps/web` — renders root component, asserts component instance is truthy
- [x] 1.5 Run `npx nx test web` and `npx nx test web --coverage` on Windows — both must exit with code 0
- [x] 1.6 Record spike result: pass (exact flag names + confirmed versions) or fail (exact error message → Jest fallback activates)
- [x] 1.7 Delete spike directory

> Spike results baked into design.md prior to implementation session.

## Phase 2: Workspace Initialization

- [x] 2.1 Pin exact Nx v21 patch version (verify latest v21.x.y at nx.dev/changelog); run `npx create-nx-workspace@<patch> ai-web-qa-tester --preset=ts --pm=npm`
  > **Deviation**: `--preset=empty` removed in v21. Used `--preset=apps` (minimal equivalent). Workspace created in temp dir then merged into existing project dir.
- [x] 2.2 Verify `package.json`: `nx` and all `@nx/*` packages have exact version strings — no `^`, `~`, or `latest`

## Phase 3: App and Lib Scaffolding

- [x] 3.1 Generate `apps/api`: `nx g @nx/nest:application api --unitTestRunner=vitest`
  > **Deviation**: `--unitTestRunner=vitest` not supported by `@nx/nest`. Used `--unitTestRunner=none` + manual Vitest config.
- [x] 3.2 Generate `apps/web`: `nx g @nx/angular:application web --unitTestRunner=vitest` (vitest per spike result)
- [x] 3.3 Generate `apps/cli`: `nx g @nx/node:application cli --unitTestRunner=vitest`
  > **Deviation**: Same as api — used `--unitTestRunner=none` + manual Vitest config.
- [x] 3.4 Generate `libs/core-domain`: `nx g @nx/js:library core-domain --bundler=tsc --unitTestRunner=vitest`
- [x] 3.5 Generate `libs/core-application`: `nx g @nx/js:library core-application --bundler=tsc --unitTestRunner=vitest`
  > **Note**: All projects scaffold at root level (not `apps/`/`libs/`). Default behavior of `--preset=apps` in Nx v21.

## Phase 4: Configuration

- [x] 4.1 Update `tsconfig.base.json`: add `"strict": true` + path aliases (aliases already generated by `@nx/js:library`)
- [x] 4.2 Add tags to each `project.json` — apps: `["type:app", "scope:platform"]`; `core-domain`: `["type:domain", "scope:core"]`; `core-application`: `["type:application", "scope:core"]`
- [x] 4.3 Add `@nx/enforce-module-boundaries` rule to root `eslint.config.mjs` with constraints
- [x] 4.4 Verify boundary enforcement: added `import '@ai-web-qa-tester/core-application'` to `core-domain/src/index.ts`; `npx nx lint core-domain` failed with `@nx/enforce-module-boundaries` error; removed import
  > **Deviation from design**: Design called for `@nestjs/common` import — that does NOT trigger the rule (external pkg). Used an Nx project import instead.

## Phase 5: Testing Infrastructure

- [x] 5.1 Add placeholder Vitest test in `libs/core-domain` and `libs/core-application` — asserts `1 + 1 === 2` (confirms runner is wired, not functionality)
  > Generated tests already present from `@nx/js:library` generator.
- [x] 5.2 Add placeholder Vitest test in `apps/api` and `apps/cli`
- [x] 5.3 Add `TestBed.createComponent()` test to `apps/web` root component — asserts component instance is truthy
  > Generated by `@nx/angular:application` generator.
- [x] 5.4 Run `npx nx run-many -t test --all` — all 5 projects must exit with code 0 ✅

## Phase 6: Verification and Documentation

- [x] 6.1 Run full validation: `npx nx run-many -t lint test build --all` — zero errors, zero warnings across all 5 projects ✅ (16 targets, all green)
- [x] 6.2 Write `docs/decisions/ADR-0001.md`: exact Nx patch version + rationale, Node/npm versions validated, Angular test runner chosen, spike outcome
- [x] 6.3 Update `openspec/config.yaml`: `strict_tdd: false → true`
- [x] 6.4 Write `docs/setup.md`: prerequisites (Node 20+ LTS, npm), install steps, verify command
